extends menuLayout

block content
    #content(data-role="content")
        label(for="searchByIdId") User ID
        input#searchByIdId(id="id", type="text")
        button#searchMinRatingSubmit(onclick="searchById()") Search
block additionalPages
